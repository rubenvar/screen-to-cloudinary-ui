{
  "name": "screen-to-cloudinary-ui",
  "version": "0.1.0",
  "description": "A UI for Screen to Cloudinary",
  "scripts": {
    "dev": "parcel serve src/index.html --open",
    "build": "parcel build src/index.html --dist-dir build --no-source-maps"
  },
  "devDependencies": {
    "@parcel/transformer-stylus": "^2.0.0-rc.0",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^6.15.0",
    "eslint-config-wesbos": "^1.0.1",
    "parcel": "^2.0.0-rc.0",
    "prettier": "^2.3.2"
  },
  "eslintConfig": {
    "extends": [
      "wesbos",
      "prettier"
    ]
  }
}
